const created = {
  successful: "has been created successfully",
};

const server = {
  ERROR: "Internal error occurred",
};

const url = {
  created: `Url ${created.successful}`,
};

module.exports = {
  url,
  server,
};
